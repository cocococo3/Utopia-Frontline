:: 纺裁部 [nobr] {"position":"2500,6600","size":"100,100"}
<<set $playerPos to {layer:6, block:"纺裁部"}>>
大量巨大的机床规律地排放在纺裁部的空间里，其上的装置规律而迅速地运动着，将一缕缕丝线合成为布匹。
<br><<set $tailoringdaily to [
	"纺裁部的人形横扛起一筒两个人那么高的布料，放置在一个镂空的凹槽内。凹槽随之上升，让布料随着重力下垂，随后对它进行拉平和切割。",
	"大量布料平整地堆叠在一起，上方的切割机正在根据版型图把它们切成不同的形状。",
	"缝纫机正在嘎嘎作响，将不同形状的布料缝合到一起。",
	"有纺裁部的人形正在把连接着环扣的厚布块折叠放置在不同布料之间，让操作缝纫机的人形将它们一起缝在衣服里。",
	"一些人形正在把制作完成的衣服熨烫平整，另一些则在把它们用带有标签的衣架悬挂起来。",
	"几个人形正推着挂满衣服的铁架，将它移动到柜台后方。",
	"几个人形正在从运输车上搬下不同颜色的丝线轴，根据不同的数量比例将它们套进纺织机里。"
]>>
<<print either($tailoringdaily)>>
<br><br>

<<if $workplace=="tailor">>
  [[工作|workintailor]]<br>
<<else>>
  [[寻求在纺裁部的工作]]<br>
<</if>>
  [[订制衣物]]<br>
  [[订制其他装备]]<br>
<<if $finishtask8>>
  [[缝补受损的衣物]]<br>
<</if>>
[[返回走廊|L6流通走廊]]


:: 寻求在纺裁部的工作
<<if $workgrade>=2>


[[愿意|agreetailor]]
[[不愿意|disagreetailor]]

<<else>>
“你的工作等级还不够，还不能加入纺裁部。”
<span style="color: grey;">【你目前的工作等级是$workgrade,需要提升到2级或者以上才能来看守部工作。】</span>

[[好吧|纺裁部]]
<</if>>

:: agreetailor

<em>你现在是纺裁部的一员了！可以随时过来工作。</em>
<<set $playerJob to "后勤">>
<<set $workplace to "tailor">>
[[继续|纺裁部]]


:: disagreetailor


[[继续|纺裁部]]


:: 订制其他装备
这里是订做除了衣物以外的装备的地方，比如管理区里常见的用于悬挂武器的绑带。在装备绑带后你就能够装备不同的武器了。

[[订制装备带]]
[[离开|纺裁部]]


:: 订制装备带

[[再考虑一下|订制其他装备]]


:: workintailor[nobr]
<<if typeof $tailorTask === "undefined">>
  <<set $tailorTask to {
    deviation: random(0, 5),
    direction: either(["left", "right"]),
    tolerance: 0,
    checked: false
  }>>
<</if>>

你走到裁切台前，面前摆着一卷待裁的布料。<br>
纺裁部一向使用激光机器来自动裁切布料，你的工作就是在机器自动送入布料前检查它是否有偏差。<br>

<<if $tailorTask.deviation === 0>>
  这批布料的裁切线毫无偏差，直接裁切就能获得工分。
<<else>>
  这批布料的裁切线偏向
  <<if $tailorTask.direction === "left">>左侧<</if>><<if $tailorTask.direction === "right">>右侧<</if>>，
  当前偏差值为 <<print $tailorTask.deviation>>。<br>
  你需要按下对应方向的调节按钮把它修正到 0；如果布料足够长，也可以在不完全修正的情况下直接裁切。
<</if>>
<br><br>

<<if $tailorTask.checked>>
  你刚才检查过，这批布料的可容忍裁切偏差约为 <<print $tailorTask.tolerance>>。
<<else>>
  你还没有确认这批布料的最大可容忍偏差。
<</if>>
<br><br>

<<if $tailorTask.deviation === 0>>
  <<link "进行裁切">>
    <<tick 15>>
    <<set $money += 8>>
    <<unset $tailorTask>>
    你顺利裁下了这卷布料，工位的验收通过后，你获得了 8 工分。
    <br><br>
    [[继续工作|workintailor]]<br>
    [[返回纺裁部|纺裁部]]
  <</link>>
<<else>>
  <<if $tailorTask.deviation <= $tailorTask.tolerance>>
    <span style="color: green;">这批布料足够长，偏差在可接受范围内，保留偏差也能完成裁切。</span><br><br>
    <<link "进行裁切（保留偏差）">>
      <<tick 20>>
      <<set $money += 6>>
      <<unset $tailorTask>>
      你判断这批布料足够长，于是保留了轻微偏差完成裁切，工位负责人认可了这批成品。你拿到了 6 工分。
      <br><br>
      [[继续工作|workintailor]]<br>
      [[返回纺裁部|纺裁部]]
    <</link>>
  <<else>>
    <span style="color: red;">这批布料不够长，偏差超过了可容忍范围，必须先修正到 0 才能裁切。</span><br><br>
  <</if>>

  <<if $tailorTask.direction === "left">>
    <<link "左调">>
      <<tick 15>>
      <<set $tailorTask.deviation = Math.max(0, $tailorTask.deviation - 1)>>
      你轻轻把布料往左校正了一点，偏差值变成了 <<print $tailorTask.deviation>>。
      <<if $tailorTask.deviation === 0>>
        <br><br>布料已经回到基准线，当前可以进行裁切。
      <</if>>
      <<goto "workintailor">>
    <</link>>
    |
    <<link "右调">>
      <<tick 15>>
      <<set $tailorTask.deviation = Math.min(5, $tailorTask.deviation + 1)>>
      你一时手滑，把布料往右推偏了一点，偏差值变成了 <<print $tailorTask.deviation>>。
      <<goto "workintailor">>
    <</link>>
  <<else>>
    <<link "左调">>
      <<tick 15>>
      <<set $tailorTask.deviation = Math.min(5, $tailorTask.deviation + 1)>>
      你把布料轻轻往左推偏了一点，偏差值变成了 <<print $tailorTask.deviation>>。
      <<goto "workintailor">>
    <</link>>
    |
    <<link "右调">>
      <<tick 15>>
      <<set $tailorTask.deviation = Math.max(0, $tailorTask.deviation - 1)>>
      你顺着正确方向把布料往右校正了一点，偏差值变成了 <<print $tailorTask.deviation>>。
      <<if $tailorTask.deviation === 0>>
        <br><br>布料已经回到基准线，当前可以进行裁切。
      <</if>>
      <<goto "workintailor">>
    <</link>>
  <</if>>
  <br><br>
  <<if $tailorTask.deviation === 0>>
    <<link "进行裁切">>
      <<tick 15>>
      <<set $money += 8>>
      <<unset $tailorTask>>
      你把偏差完全修正后顺利完成裁切，工位验收确认后，你获得了 8 工分。
      <br><br>
      [[继续工作|workintailor]]<br>
      [[返回纺裁部|纺裁部]]
    <</link>>
  <</if>>
<</if>>
<br><br>

<<link "检查这批布料的可容忍裁切偏差">>
  <<roll $hiding>>
  <<if $success>>
    <<set $tailorTask.checked to true>>
    <<set $tailorTask.tolerance to random(0, 5)>>
    你快速核对了样本标尺，判断这批布料的可容忍裁切偏差大约为 <<print $tailorTask.tolerance>>。
  <<else>>
    你反复比对了布料纹路和刻度，仍然只能粗略判断，没法完全确认它的允许偏差。
  <</if>>
  <<goto "workintailor">>
<</link>><<prob $hiding><br>
[[返回纺裁部|纺裁部]]





:: 纺裁部widget [widget nobr] 

<<widget "recalc">>
<<run
(function(){
  const hem   = $hemOpts[$draft.hemIdx];
  const slv   = $slvOpts[$draft.slvIdx];
  const fab   = $fabOpts[$draft.fabIdx];
  const overS = $draft.overskirt ? "存在" : "无";

  $draft.hemMod = hem.mod;
  $draft.slvMod = slv.mod;

  /* 1️⃣ 服装种类：按编号范围判断 */
  let hit = null;
  for (const p of $presets) {
    const hemOK = ($draft.hemIdx >= p.hem.min && $draft.hemIdx <= p.hem.max);
    const slvOK = ($draft.slvIdx >= p.sleeve.min && $draft.slvIdx <= p.sleeve.max);
    const fabOK = ($draft.fabIdx >= p.fabric.min && $draft.fabIdx <= p.fabric.max);
    const overOK = (p.over === "任意") || (p.over === overS);
    if (hemOK && slvOK && fabOK && overOK) { hit = p; break; }
  }
  if (!hit) hit = $fallbackPreset;
  $draft.name  = hit.name;
  $draft.intro = hit.intro;

  /* 2️⃣ 计算可承受磨损度（用修正值） */
  let maxw = fab.baseMax + $draft.hemMod + $draft.slvMod + ($draft.overskirt ? 9 : 0);
  if (maxw < 0) maxw = 0;
  $draft.maxworeout = maxw;

  /* 3️⃣ 成本公式（按编号） */
  const baseCost = fab.baseMoney;
  const hemCost  = $draft.hemIdx * 5 * fab.baseMoney;
  const slvCost  = $draft.slvIdx * 2 * fab.baseMoney;
  const overCost = $draft.overskirt ? 20 : 0;
  $draft.cost    = baseCost + hemCost + slvCost + overCost;
})();
>>
<</widget>>

<<widget "hemTag">><<print $hemOpts[$args[0]].name>><</widget>>
<<widget "slvTag">><<print $slvOpts[$args[0]].name>><</widget>>
<<widget "ensureDefaultDraft">>
  <<set _defaultPreset to ($presets || []).find(p => String(p.name).includes("普通后勤部门制服")) || (($presets && $presets[0]) || {
    name: "改良制服",
    intro: "这是你定制的服装。",
    hem: { min: 4, max: 5 },
    sleeve: { min: 3, max: 4 },
    fabric: { min: 2, max: 2 },
    over: "任意"
  })>>
  <<if !$draft || !$draft.hemIdx || !$draft.slvIdx || !$draft.fabIdx || $draft.name === "" || ($draft.cost === 0 && !$draft.name)>>
    <<set $draft to {
      hemIdx: _defaultPreset.hem.min,
      slvIdx: _defaultPreset.sleeve.min,
      fabIdx: _defaultPreset.fabric.min,
      overskirt: _defaultPreset.over === "存在",
      hemMod: 0,
      slvMod: 0,
      maxworeout: 0,
      cost: 0,
      name: _defaultPreset.name,
      intro: _defaultPreset.intro
    }>>
  <</if>>
<</widget>>

:: 制作确认 {"position":"2975,6750","size":"100,100"}
你将制作： ''<<print $draft.name>>''  
简介：<<print $draft.intro>>

- 下摆： <<hemTag $draft.hemIdx>>
- 袖部： <<slvTag $draft.slvIdx>>
- 布料： <<print $fabOpts[$draft.fabIdx].name>>
- 外裙： <<print $draft.overskirt ? "有" : "无">>
- 可承受磨损度： ''<<print $draft.maxworeout>>''
- 成本： ''<<print $draft.cost>> money''

<<link "支付并制作">>
  <<set $money -= $draft.cost>>
  <<set _cloth to {
      name: $draft.name,
      stats: {
        hemIdx: $draft.hemIdx,
        slvIdx: $draft.slvIdx,
        fabIdx: $draft.fabIdx,
        overskirt: $draft.overskirt
      },
      intro: $draft.intro,
      clothdirty: 0,
      clothworeout: 0,
      maxworeout: $draft.maxworeout
  }>>
  <<run $wardrobe.push(_cloth)>>
  <<goto "制作完成">>
<</link>> 
<<link "返回修改">><<goto "订制衣物">><</link>>
[[放弃订制|纺裁部]]


:: 制作完成 {"position":"2975,6950","size":"100,100"}
制作完成！已扣除 ''<<print $draft.cost>> 工分''  

<<link "查看衣柜">><<goto "衣柜">><</link>> 
[[继续订制|订制衣物]]
<<link "返回纺裁部">><<goto "纺裁部">><</link>>


:: 订制衣物[nobr] {"position":"2800,6850","size":"100,100"}
<<ensureDefaultDraft>><<recalc>>
你正站在一块大约有一米宽的可触屏幕前，看着上面显示出的种种有关衣物的设定，不同的衣物性能——包括袖长、裙长和布料的结实程度——都用不同的数字代替。<br><br>
名称： ''<<print $draft.name>>''  <br><br>
“ <<print $draft.intro>>”

- 下摆： <<hemTag $draft.hemIdx>>（编号 <<print $draft.hemIdx>>）<br>
- 袖部： <<slvTag $draft.slvIdx>>（编号 <<print $draft.slvIdx>>）<br>
- 布料： <<print $fabOpts[$draft.fabIdx].name>>（编号 <<print $draft.fabIdx>>）<br>
- 外裙： <<print $draft.overskirt ? "有" : "无">><br>
- 耐久： <<print $draft.maxworeout>><br>
- 成本： <<print $draft.cost>> 工分<br><br>

——下摆——<br>
<<link "剪短下摆">>
  <<set $draft.hemIdx = Math.max(1, $draft.hemIdx - 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>> <br>
<<link "加长下摆">>
  <<set $draft.hemIdx = Math.min(5, $draft.hemIdx + 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>><br><br>

——袖部——<br>
<<link "剪短袖部">>
  <<set $draft.slvIdx = Math.max(1, $draft.slvIdx - 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>> <br>
<<link "加长袖部">>
  <<set $draft.slvIdx = Math.min(4, $draft.slvIdx + 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>><br>

——布料类型——<br>
<<link "更结实的布料">>
  <<set $draft.fabIdx = Math.min(3, $draft.fabIdx + 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>> <br>
<<link "更轻便的布料">>
  <<set $draft.fabIdx = Math.max(1, $draft.fabIdx - 1)>>
  <<recalc>><<goto "订制衣物">>
<</link>><br>

——外裙订制——<br>
<<if !$draft.overskirt>>
  <<link "增加外裙">>
    <<set $draft.overskirt = true>>
    <<recalc>><<goto "订制衣物">>
  <</link>>
<<else>>
  <<link "取消外裙">>
    <<set $draft.overskirt = false>>
    <<recalc>><<goto "订制衣物">>
  <</link>>
<</if>>
<hr>
<br>
<<link "确认制作">>
  <<if $money < $draft.cost>>
    <<goto "资金不足">>
  <<else>>
    <<goto "制作确认">>
  <</if>>
<</link>> <br>
<br>

[[放弃订制|纺裁部]]


:: 资金不足 {"position":"2975,6850","size":"100,100"}
你的余额为 ''<<print $money>> money''，不足以支付本次制作所需的 ''<<print $draft.cost>> money''。

<<link "返回订制衣物">><<goto "订制衣物">><</link>>
[[放弃订制|纺裁部]]

:: 缝补受损的衣物





[[纺裁部]]